#include <bits/stdc++.h>
using namespace std;
bool check(str)

int main() {
   string s ;
   getline(cin, s) ;
   vector<string> v ;
   stringstream ss(s) ;
   string tmp ;{
   while(ss >> tmp)
    v.push_back(tmp);
   }
   reverse(v.begin(), v.end());
   for(string x : v) cout << x << ' ';
 
}